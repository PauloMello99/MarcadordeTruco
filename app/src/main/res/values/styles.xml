<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Base.Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorBkgStart</item>
        <item name="colorPrimaryDark">@color/colorBkgStart</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="TeamNameTextView">

        <item name="android:gravity">center</item>
        <item name="android:layout_marginTop">16dp</item>
        <item name="android:textColor">@color/colorLabel</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:textSize">22sp</item>
    </style>

    <style name="TeamScoreTextView" parent="TeamNameTextView">
        <item name="android:textSize">78sp</item>
    </style>

    <style name="ButtonOutlineBorder">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginBottom">10dp</item>
        <item name="android:layout_marginLeft">16dp</item>
        <item name="android:layout_marginRight">16dp</item>
        <item name="android:textSize">22sp</item>
        <item name="android:textColor">@color/colorLabel</item>
        <item name="android:background">@drawable/button_outline_border</item>
    </style>

</resources>
